# AlegroCode backend — install with: pip install -r backend/requirements.txt

# Web framework
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*
pydantic>=2.7
pydantic-settings>=2.3

# Jupyter-friendly async
nest_asyncio>=1.6

# Persistence
SQLAlchemy>=2.0
aiosqlite>=0.20

# Scheduler for scraper
APScheduler>=3.10

# ML / Computer Vision
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.44,<4.46
accelerate>=0.33,<0.35
tokenizers>=0.19,<0.20
huggingface-hub>=0.24,<1.0

# Segmentation models
ultralytics>=8.2.0
rembg[cpu]>=2.0.50
# SAM2 — install from git (one-off):
# pip install git+https://github.com/facebookresearch/sam2.git

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.26,<2.0
Pillow>=10.0.0
matplotlib>=3.8,<3.11
scikit-image>=0.22

# Inpainting
simple-lama-inpainting>=0.1.2
diffusers>=0.27
safetensors>=0.4

# Scraper stack (bypasses basic TLS fingerprinting)
curl_cffi>=0.7
httpx>=0.27
selectolax>=0.3.21
tenacity>=8.2
pyyaml>=6.0
lxml>=5.2

# Tunnel for rented-GPU Jupyter
pyngrok>=7.0.0

# Utilities
modelscope>=1.10.0
